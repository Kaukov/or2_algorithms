const _           = require('lodash')
const CLI         = require('clui')
const Spinner     = CLI.Spinner
const chalk       = require('chalk')
const inquirer    = require('../lib/inquirer')

class task4 {
  static solve ({ x0, a1, b1, a2, b2, kmax, epsx, epsf, c }) {
    const con = c || 1
    // x0 = (x10, x20)
    // a1, b1, a2, b2
    // c = const = 1
    // kmax
    // epsx
    // epsf

    // Rok = c / (k + 1)
  }
}

function calculateGradient () {
  // deltaf(xk) = (2(x1 - 1))
  //              (2(x2 - 2))

  // (n) =      n!
  // ( ) = -------------
  // (k) = k! * (n - 1)!
}

function calculateGradientNorm () {
  //
}

function calculateGamma () {
  //
}

function calculateRo (k, c) {
  //
}

function calculateXj () {
  //
}

module.exports.task3
